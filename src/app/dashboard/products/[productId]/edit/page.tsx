'use client'

import React from 'react'
import { Layout, ProductForm } from '@/components'

export default function Edit() {
  return (
    <Layout>
      <ProductForm onSubmit={() => {}} title="Tambah Produk" />
    </Layout>
  )
}
